# Core BOM management dependencies
openpyxl>=3.1.0
pandas>=2.3.0
python-dotenv>=1.0.0
requests>=2.32.0
xlrd>=2.0.1

# Core application dependencies
# Note: ESPHome has strict version pins for its dependencies
# Let ESPHome manage click, pillow, and other transitive deps
esphome>=2025.10.5
rich>=13.0.0
starlette<0.47 # Constrain starlette to be compatible with platformio

# Development and testing tools
detect-secrets>=1.5.0
pre-commit>=3.0.0
pip-tools>=6.0.0
pytest>=8.0.0
pytest-mock>=3.12.0
flake8>=7.0.0
yamllint>=1.32.0
ruff>=0.12.9
black
isort
